<form method="post" action="{{ path('app_mail_delete', {'id': mail.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mail.id) }}">
    <button class="btn">Delete</button>
</form>
